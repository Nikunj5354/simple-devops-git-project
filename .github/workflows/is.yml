name: Simple CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

       - name: Run fixed command
        run: |
          echo "CI pipeline restarted"
          ls
          echo "Pipeline fixed successfully"



